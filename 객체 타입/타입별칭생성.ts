/*
타입별칭이란 타입을 재사용하고 이름을 지정하는 방법을 말한다 
새 타입 키워드 그다음에 타입에 붙일 이름 그리고 등호를 작성하면 된다.
반복되는 코드를 줄일수 있다.
*/

// type Point = {
//   x: number;
//   y: number;
// };

// let coordinate: Point = { x: 34, y: 2 };

// function randomCoordinate(): Point {
//   return { x: Math.random(), y: Math.random() };
// }

/*
객체타입 뿐만아니라 원시타입도 만들수 있다 
*/
